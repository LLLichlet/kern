// exit: 0

type Base[T] = trait {
    make: fn() T,
};

type Derived: Base[Apply[i32]] = trait {
    type Apply[T];
};

type Id = struct {};

impl *Id: Base[i32] {
    fn make() i32 {
        return 7;
    }
}

impl *Id: Derived {
    type Apply[T] = T;
}

fn main() i32 {
    let id = Id.{};
    let derived = *Derived.{ id.& };
    return derived.make() - 7;
}
