// exit: 0

trait Base[T] {
    fn value() T;
};

trait Derived: Base[Apply[i32]] {
    type Apply[T];
};

struct Id {};

impl &Id: Base[i32] {
    fn value() i32 {
        return 7;
    }
}

impl &Id: Derived {
    type Apply[T] = T;
}

fn main() i32 {
    let id = Id.{};
    let derived = &Derived.{ id.& };
    let base = &Base[i32].{ derived };
    return base.value() - 7;
}
