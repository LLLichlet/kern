// class: modules-inline-visibility
// source: private consts inside inline modules must not leak through public module paths
// stderr: module has no visible member `SECRET`

mod api {
    const SECRET = i32.{1};
}

fn main() i32 {
    return api.SECRET;
}
