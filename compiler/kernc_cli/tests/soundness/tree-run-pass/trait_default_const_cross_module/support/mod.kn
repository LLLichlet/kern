pub const BASE = 3usize;

pub const fn bump[N: usize]() usize {
    return N + BASE;
}

pub trait SizedValue[N: usize] {
    fn value() usize {
        return bump[N]();
    }
};

pub struct Item {};

impl Item: SizedValue[4] {}
