[package]
name = "bitio"
version = "0.1.0"
kern = "0.7.2"
publish = false
description = "Bit and endian utilities for Kern protocol and codec libraries"
readme = "README.md"
license = "MIT"

[lib]
root = "src/lib.rn"

[test]
roots = ["tests/smoke.rn"]
