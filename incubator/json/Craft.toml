[package]
name = "json"
version = "0.1.0"
kern = "0.7.3"
publish = false
description = "Borrowed JSON validation, slicing, and compact rendering for Kern"
readme = "README.md"
license = "MIT"

[lib]
root = "src/lib.rn"

[example]
roots = [
    "examples/hello_compact.rn",
    "examples/bench_json.rn",
]

[test]
roots = ["tests/smoke.rn"]
