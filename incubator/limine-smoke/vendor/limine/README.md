This is a local Limine-shaped stub tree used to exercise `craft` package
resources offline.

It is intentionally not a real bootloader distribution.

Replace `[resources].limine` in `Craft.toml` with the real upstream git source
when you want to test remote fetch behavior.
