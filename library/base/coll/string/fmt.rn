//! `Printable` integration for strings.

use std.io.{Printable, Writer};
use ..String;

impl *String : Printable {
    /// Writes the raw string bytes into `writer`.
    pub fn fmt(writer: *mut Writer) void {
        let _ = writer.write(self.as_str());
    }
}
