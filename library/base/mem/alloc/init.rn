//! Foundation allocators.
//!
//! `base.mem.alloc` owns allocator traits and reusable heap strategies. Raw
//! page acquisition belongs to hosted consumers such as `std.mem`.

mod allocator;
mod typed;
mod gpa;
mod arena;

pub use .allocator.{Allocator, AllocError};
pub use .gpa.{GPA, GpaBuilder, gpa};
pub use .arena.{Arena, ArenaBuilder, ArenaMark, arena};
