//! Message, diagnostics, and fail-fast helpers.
//!
//! This module defines the runtime-facing helpers used for assertions, traps,
//! and lightweight diagnostic output.

mod runtime;
pub use .runtime.{write, fail, trap, panic, assert, log, debug};
