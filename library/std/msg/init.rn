//! Message, diagnostics, and fail-fast helpers.
//!
//! This module defines runtime-facing diagnostics and fail-fast helpers.
//! Freestanding test assertions live in `base.test`.

mod runtime;
pub use .runtime.{fail, panic, log, debug};
