//! Formatting traits.

/// A byte sink used by the formatting subsystem.
pub type Writer = trait {
    write: fn([]u8) usize,
};

/// A value that knows how to render itself into a `Writer`.
pub type Printable = trait {
    fmt: fn(*mut Writer) void,
};
