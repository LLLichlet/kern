fn main(args: [][]u8) i32 {
    if (#args > 1) {
        let name = args.[1];
        std.io.println("hello, {}!", .{ name.&, });
    } else {
        std.io.println("hello, world!", .{});
    }
    0
}