extern {
    fn printf(format: *u8, ...) i32;
}

// public 函数，允许外部调用
pub fn print_point(x: i32, y: i32) void {
    printf("Point(x: %d, y: %d)\n\0" as *u8, x, y);
}

// 私有函数，拦截测试
fn hidden_util() void {
    printf("You should not see this.\n\0" as *u8);
}