// compile-flags: --link-libc
// expected-error: field `y` is missing and has no default value

type Point = struct {
    x: i32,
    y: i32,
};

fn main() i32 {
    let p = Point.{ x: 10 }; // `y`
    0
}